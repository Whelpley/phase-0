var stack = [
    "wub wub wub",
    "oontz oontz oontz",
    "doo wap doo wah",
    "don\'t Snapchat that duckface",
    "boots and pants"
  ];

function shuffle(array){
  var copy = array;
  var result = [],

  for (var counter = array.length, counter > 0, counter--) {
 //choose rand index within counter length
    var index = Math.floor(Math.random() * counter);
    copy[index]
  }

}